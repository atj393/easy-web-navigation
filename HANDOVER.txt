Easy Web Navigation - HANDOVER
==============================

This file is plain-text memory for continuing in a fresh session. Keep it
updated at the end of every prompt iteration.

PROJECT NAME
  Easy Web Navigation (formerly "KeyWise Web") - a keyboard accessibility
  companion browser extension for Chrome, Edge, and Firefox. Inspection /
  assistive browsing tool. NOT an overlay product, NOT an auto-fixer, NOT a
  legal compliance certifier.
  Tagline (popup): Check whether a page is easy to use with a keyboard.

CURRENT PHASE
  Phase 1A-UX - simplify + enlarge the popup for non-technical users.
  UI wording + visual hierarchy only. No behavior change.

CURRENT BRANCH
  main

LATEST COMMIT HASH
  (Run `git log -1 --pretty=fuller`. The Phase 1A-UX commit message is:
   "feat: simplify popup guidance for everyday users".
   Prior baselines: 1A = 2f62d78, 0H-Fix = be0956e, 0H = 2f81470, 0G = 5629d66,
   0F = c27a543, 0P = 7f8cfed, 0E = 17b9e02, 0N rename = 7e7dd28, 0D = 54d3651,
   0C = 8cd154d, 0B = 1890b49, 0A = 54dcba4.)

COMMIT AUTHOR
  atj393 <alexistoby393@gmail.com>  (local git identity set for this repo)
  No Co-Authored-By. No generated-by trailers. No assistant attribution.

DATE/TIME OF UPDATE
  2026-06-20 (Phase 1A-UX).

REPO PATH
  c:\Alexis\Test\browser\keyboard-extension\keywise-web
  NOTE: local root folder still named "keywise-web" (not renamed).

GITHUB REPOSITORY URL
  https://github.com/atj393/easy-web-navigation  (PUBLIC)
  Remote: origin -> https://github.com/atj393/easy-web-navigation.git

PUSH STATUS
  Phase 1A-UX committed and pushed to origin/main.

SUMMARY OF WHAT CHANGED (popup wording + layout only)
  Popup wording (apps/extension/entrypoints/popup/App.tsx):
  - Tagline -> "Check whether a page is easy to use with a keyboard."
  - Status: "Ready to check this page." / "Checking this page…" / "N problem(s) found".
  - Main action: "Scan current page" -> "Check this page".
  - New "Show on this page" section with two guides:
    * "Keyboard focus highlight" + buttons "Show/Hide keyboard focus" (was Focus helper).
    * "Keyboard path" + buttons "Show/Hide keyboard path" (was Tab path).
  - New "Keep checking as you browse" section (was Monitoring):
    * "Automatic checking: <Off | On for this page | On for this website | On for all websites>".
    * "Where should I keep checking?" select with options
      "This page only / This website / All websites" (internal values unchanged:
      current-tab / site / all-sites).
    * One dynamic explanation per scope; all-sites shows a permission heads-up before start;
      enabled note about keyboard focus/path re-applying.
    * Buttons "Start/Stop automatic checking".
  - Results: heading "What we checked"; cards "Keyboard use / Moving around the page /
    Names and labels"; "Problems found"; "Show this problem" (was Locate on page);
    "Copy results" / "Download results"; plain empty/zero-problem text; new status messages.
  - Footer disclaimer: "This tool checks for possible keyboard-access problems. It does not
    change the website or confirm legal compliance."
  Popup sizing/readability (style.css):
  - width min(420px, calc(100vw - 32px)); padding 20px; section gap 16px.
  - body 13px / controls 14px / descriptions line-height ~1.5; primary buttons >=44px.
  - clear 3px focus outlines; restrained section dividers (no heavy floating cards);
    light/dark preserved. Removed obsolete .monitor*/.popup__tools CSS; added
    .block/.guide/.field classes.
  Display helpers (apps/extension/lib/monitoring.ts):
  - Added scopeChoiceLabel, automaticCheckingStatusLabel, scopeExplanation (pure, popup-only).
    scopeLabel kept for back-compat. No business-logic change.
  Docs: README, reviewer-test-instructions, manual-qa-before-submit, screenshot-plan,
  edge-add-ons-listing, chrome-web-store-listing, demo-plan updated to the new labels.

FILES CHANGED
  apps/extension/entrypoints/popup/App.tsx
  apps/extension/entrypoints/popup/style.css
  apps/extension/lib/monitoring.ts
  apps/extension/lib/monitoring.test.ts
  README.md, CHANGELOG.md, HANDOVER.txt
  docs/demo-plan.md, docs/store/reviewer-test-instructions.md,
  docs/store/manual-qa-before-submit.md, docs/store/screenshot-plan.md,
  docs/store/edge-add-ons-listing.md, docs/store/chrome-web-store-listing.md

COMMANDS RUN
  git status/branch/log; node v24.13.0; pnpm 9.15.9
  pnpm typecheck -> PASS; pnpm lint -> PASS; pnpm test -> PASS (9 files, 113)
  pnpm build + build:firefox -> PASS; pnpm run ci -> PASS; pnpm release:all -> PASS
  manifest inspect -> permissions unchanged
  rg old-wording audit (see below)

TEST RESULTS
  113 tests across 9 files, all passing. New: 4 popup-display-label tests in
  monitoring.test.ts (scopeChoiceLabel, automaticCheckingStatusLabel,
  scopeExplanation, no-technical-terms). Existing monitoring/scanner/overlay/
  tab-path/report/clipboard/spa tests unchanged and passing.

EXPLICIT BEHAVIOR NOTE
  No scanner, monitoring, permissions, overlays, or page behavior changed. Only
  user-facing popup text + layout/styling + popup-only display helpers. Internal
  scope values (current-tab/site/all-sites) and persisted keys
  (focusHelperEnabled/tabPathEnabled/monitoringEnabled/monitoringScope) unchanged.

PERMISSION CHANGES
  NONE. Required: activeTab, scripting, storage. optional_host_permissions:
  http://*/*, https://*/*. No required host permissions.

RELEASE ARTIFACT STATUS
  Regenerated via `pnpm release:all` (git-ignored):
  artifacts/chrome/easy-web-navigation-chrome-v0.1.0.zip  (manifest at root: YES)
  artifacts/edge/easy-web-navigation-edge-v0.1.0.zip      (manifest at root: YES)

OLD-WORDING SEARCH
  No old popup labels visible in the popup UI. "Focus helper" / "tab path" /
  "Monitoring" / "scope" do not appear in active popup UI text. Remaining mentions
  are internal code identifiers/comments and historical CHANGELOG/HANDOVER notes,
  which is acceptable.

KNOWN LIMITATIONS
  - Real product screenshots still pending (placeholder + plan in docs/store).
  - Manual cross-browser QA still required before store submission.
  - SPA refresh best-effort; missing-visible-focus deferred; Firefox MV2.
  - Local folder still keywise-web.

WHAT WAS INTENTIONALLY NOT IMPLEMENTED
  - No new features, no scanner-rule/monitoring/permission changes.
  - No AI/speech/analytics/remote calls/auto-fix/ARIA injection/page mutation.
  - No store submission.

NEXT RECOMMENDED PHASE
  Phase 1B - capture real screenshots into docs/store/assets, run manual
  cross-browser QA, then tag v0.1.0 + GitHub Release; store submission stays manual.

NEXT RECOMMENDED PROMPT SUMMARY
  "Phase 1B: Capture real popup screenshots (new plain-language UI) into
   docs/store/assets per screenshot-plan.md, run manual-qa-before-submit.md in
   Chrome and Edge, then create git tag v0.1.0 and a GitHub Release attaching the
   artifacts/chrome and artifacts/edge ZIPs (gh release). Keep store submission
   manual; no code/feature/permission changes. Commits authored by atj393 (no
   Co-Authored-By). Update HANDOVER.txt and CHANGELOG.md; push to origin."
