Easy Web Navigation - HANDOVER
==============================

This file is plain-text memory for continuing in a fresh session. Keep it
updated at the end of every prompt iteration.

PROJECT NAME
  Easy Web Navigation (formerly "KeyWise Web") - a keyboard accessibility
  companion browser extension for Chrome, Edge, and Firefox. Inspection /
  assistive browsing tool. NOT an overlay product, NOT an auto-fixer, NOT a
  legal compliance certifier.
  Tagline: A keyboard accessibility companion for the web.

CURRENT PHASE
  Phase 0N - project rename (KeyWise Web -> Easy Web Navigation). Rename-only;
  no feature/permission/logic changes beyond rename-driven imports/config.

CURRENT BRANCH
  main

LATEST COMMIT HASH
  (Run `git log -1 --pretty=fuller`. The Phase 0N commit message is:
   "chore: rename project to Easy Web Navigation".
   Prior baselines: 0D = 4894c5e, 0C = 2bab9ad, 0B = 6efbfb5, 0A = 5f66f62.)

COMMIT AUTHOR
  atj393 <alexistoby393@gmail.com>  (local git identity set for this repo)
  No Co-Authored-By. No generated-by trailers. No assistant attribution.

DATE/TIME OF UPDATE
  2026-06-10 (Phase 0N).

REPO PATH
  c:\Alexis\Test\browser\keyboard-extension\keywise-web
  NOTE: the local root folder is still named "keywise-web". It was NOT renamed
  (cannot rename the folder while running inside it). It can be renamed to
  "easy-web-navigation" manually later from the parent directory; .git and
  generated output folders should not be renamed.

NAMING (FINAL)
  Product display name: Easy Web Navigation
  Repository slug:      easy-web-navigation
  Package scope:        @easy-web-navigation
  Tagline:              A keyboard accessibility companion for the web.

SUMMARY OF WHAT CHANGED
  - Replaced the product name everywhere: KeyWise Web -> Easy Web Navigation.
  - Replaced package scope @keywise -> @easy-web-navigation across all package
    names, internal dependencies, and imports.
  - Replaced slug keywise-web -> easy-web-navigation.
  - Renamed internal identifiers: __keywiseWebInitialized ->
    __easyWebNavigationInitialized; data-keywise-overlay attribute ->
    data-easy-web-navigation-overlay; export filename keywise-report.md ->
    easy-web-navigation-report.md.
  - Extension manifest name -> "Easy Web Navigation"; description ->
    "A keyboard accessibility companion for inspecting focus, tab order, and
    navigation issues on web pages."
  - Popup shows product name + tagline; options shows product name.
  - Added PRODUCT_NAME / PRODUCT_TAGLINE constants in shared-types; popup uses
    them; added a shared-types test asserting them.
  - Updated README, all docs, CHANGELOG (Phase 0N entry), HANDOVER.
  - Ran pnpm install to refresh pnpm-lock.yaml and relink workspace scopes.

REPLACEMENT METHOD / DECISIONS
  - Applied an ordered, mechanical rename via perl over `git ls-files`
    (excluding HANDOVER.txt and pnpm-lock.yaml), order chosen to avoid breaking
    identifiers: @keywise/ -> @easy-web-navigation/; __keywiseWebInitialized;
    data-keywise-overlay; keywise-report; keywise-web; "KeyWise Web";
    "KeyWise"; "Keywise".
  - "keywise"/"KeyWise" was exclusively the brand token, so replacement was safe
    and did NOT touch WCAG/keyboard/navigation/standards wording.
  - No exported TS symbols contained "KeyWise", so none were renamed (only
    string/comment/identifier-prefix occurrences).
  - The only intentional old-name mentions remaining are in CHANGELOG (the
    Phase 0N rename note) and HANDOVER (this rename history).

FILES AND FOLDERS CHANGED (rename touched ~40 files)
  Root: package.json (name easy-web-navigation), README.md, CHANGELOG.md,
        HANDOVER.txt, eslint.config.js, pnpm-lock.yaml
  Extension: apps/extension/package.json (@easy-web-navigation/extension + deps),
        wxt.config.ts (name + description), entrypoints/background.ts,
        entrypoints/content.ts (flag + comments + imports),
        entrypoints/popup/{App.tsx,index.html,style.css},
        entrypoints/options/{App.tsx,index.html}, lib/settings.ts
  Demo: apps/demo-sites/package.json (@easy-web-navigation/demo-sites) + the
        five demo HTML pages + index.html
  Packages (name + deps + imports + comments):
        shared-types (+PRODUCT_NAME/PRODUCT_TAGLINE +test), wcag-rules
        (+criteria.ts), dom-scanner, keyboard-engine, focus-overlay
        (data-* attribute + tests), report-generator (tool name + tests)
  Docs: docs/{architecture,limitations,roadmap,demo-plan,wcag-keyboard-profile}.md
  No source files/folders were named with "keywise"; none needed git mv.

COMMANDS RUN
  Inspect: git status/branch/log, node v24.13.0, pnpm 9.15.9
  rg searches for old/new names (see "OLD-NAME SEARCH" below)
  pnpm install        -> OK (lock refreshed, scopes relinked)
  pnpm typecheck      -> PASS
  pnpm lint           -> PASS
  pnpm test           -> PASS (6 files, 68 tests)
  pnpm build          -> PASS (chrome-mv3; permissions unchanged:
                         activeTab/scripting/storage; no host_permissions)
  pnpm build:firefox  -> PASS (firefox-mv2)
  pnpm run ci         -> PASS

TEST RESULTS
  68 tests passing across 6 files (added 1 product-name constant test).
  Existing scanner/rule/report/overlay/keyboard-engine tests pass with renamed
  package specifiers and the data-easy-web-navigation-overlay attribute.

OLD-NAME SEARCH (after edits)
  rg "KeyWise|Keywise|keywise|@keywise|KEYWISE" (excl node_modules/.git/.output)
  -> only CHANGELOG.md (Phase 0N rename note) and HANDOVER.txt (history).
  rg "Easy Web Navigation|easy-web-navigation|@easy-web-navigation"
  -> present in manifest/config, popup, options, docs, package metadata, tests.

KNOWN LIMITATIONS
  - Local root folder still named keywise-web (rename manually later).
  - GitHub remote (if/when created) should use the slug easy-web-navigation.
  - Firefox build is MV2 (WXT default); Chrome/Edge MV3. No icons yet.
  - missing-visible-focus still deferred (unchanged by this phase).

WHAT WAS INTENTIONALLY NOT IMPLEMENTED
  - No new features, no scanner/overlay/tab-path logic changes (only rename
    -driven imports/config).
  - No permission changes. No AI, no speech, no auto-fix, no ARIA injection,
    no legal compliance claims.
  - No root-folder rename, no .git rename, no remote, no publish, no PR.

MANUAL FOLLOW-UP
  - Optionally rename the local folder from the parent dir:
      (from c:\Alexis\Test\browser\keyboard-extension) move/rename
      "keywise-web" -> "easy-web-navigation".
  - If a GitHub remote is created, name it "easy-web-navigation".

NEXT RECOMMENDED PHASE
  Phase 0E - developer report export polish (copy-to-clipboard; optional
  tab-path summary in the report, clearly labeled as a runtime visual aid).
  Still read-only, no compliance claims.

NEXT RECOMMENDED PROMPT SUMMARY
  "Phase 0E: Improve developer report export in @easy-web-navigation/report-
   generator. Add copy-to-clipboard in the popup and optionally include the
   current tab-path summary (counts only, labeled as a runtime visual aid, not
   an audit metric). Keep the non-compliance disclaimer prominent; keep
   everything read-only; no broad host permissions, no AI, no speech. Add tests
   for the new report content and clipboard fallback. Keep commits authored by
   atj393 (no Co-Authored-By). Update HANDOVER.txt and CHANGELOG.md."
